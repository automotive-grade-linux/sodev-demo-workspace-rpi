# CONFIG_IOREQ_SERVER=y is required so Xen can forward guest MMIO/PCI accesses
# to qemu; without it the qemu virtio-X-pci backend never reaches the guest
# (empty /sys/bus/virtio/devices/, missing /dev/dri, weston drm-backend fatal).
CONFIG_IOREQ_SERVER=y
